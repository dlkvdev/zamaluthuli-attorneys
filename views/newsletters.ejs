<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ZLA - Newsletters</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="icon" type="image/x-icon" href="/MEDIA/backend/favicon.png">
    <style>
    html, body {
      height: 100%;
      margin: 0;
      padding: 0;
    }

    .page-container {
      display: flex;
      flex-direction: column;
      min-height: 100vh; /* Ensures the container takes at least the full viewport height */
    }

    .content {
      flex: 1 0 auto; /* Allows the main content to grow and push the footer down */
      padding:20px;
    }

    footer {
      flex-shrink: 0; /* Prevents the footer from shrinking */
      background-color: #f8f8f8;
      padding: 20px;
      text-align: center;
    }

    /* Styling for newsletter items */
    .newsletter-item {
      margin-bottom: 20px;
    }

    .newsletter-item h3 {
      margin: 0 0 10px 0;
    }

    .newsletter-item p {
      margin: 5px 0;
    }

    .newsletter-item a {
      color: #007bff;
      text-decoration: none;
    }

    .newsletter-item a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
    <!-- Header -->
    <div class="page-container">
     <%- include('partials/header') %>

    <main class="content">
      <h1>Newsletters</h1>

      <% if (newsletters && newsletters.length > 0) { %>
        <% newsletters.forEach(newsletter => { %>
          <div class="newsletter-item">
            <h3><%= newsletter.title %></h3>
            <% if (newsletter.date) { %>
              <p>Uploaded: <%= newsletter.date %></p>
            <% } %>
            <% if (newsletter.fileId && newsletter.fileId !== 'null') { %>
              <a href="/file/<%= newsletter.fileId %>" class="btn btn-warning" style="color:black">Download PDF</a>
            <% } else { %>
              <p>No file available</p>
            <% } %>
          </div>
        <% }); %>
      <% } else { %>
        <p>No newsletters available at this time.</p>
      <% } %>
    </main>

   
  </div>
   <footer>
        <%- include('partials/footer') %>
    </footer>
</body>
</html>

