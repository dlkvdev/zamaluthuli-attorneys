<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Manage Team - Zama Luthuli Attorneys</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <style>
    html, body {
      height: 100%;
      margin: 0;
    }
    body {
      background-image: url('/MEDIA/wallpaper.jpg');
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
      display: flex;
      flex-direction: column;
      font-family: 'Roboto', sans-serif;
    }
    .content {
      flex: 1 0 auto;
    }
    .dashboard-container {
      display: flex;
      min-height: calc(100vh - 80px);
    }
    .sidebar {
      width: 250px;
      background: rgba(44, 62, 80, 0.95);
      padding: 20px 0;
      position: fixed;
      height: 100%;
      transition: transform 0.3s ease;
    }
    .sidebar .logo {
      max-width: 150px;
      margin: 20px auto;
      display: block;
    }
    .sidebar ul {
      list-style: none;
      padding: 0;
    }
    .sidebar ul li {
      padding: 15px 20px;
      transition: background-color 0.3s;
    }
    .sidebar ul li a {
      color: #e0e0e0;
      text-decoration: none;
      font-size: 1.1rem;
    }
    .sidebar ul li:hover {
      background-color: #34495e;
    }
    .main-content {
      margin-left: 250px;
      padding: 20px;
      width: calc(100% - 250px);
      background: rgba(236, 240, 241, 0.95);
    }
    .form-card, .list-card {
      background: #34495e;
      padding: 20px;
      border-radius: 10px;
      margin-bottom: 20px;
    }
    .form-card h2, .list-card h2 {
      color: #00d1b2;
      margin-top: 0;
    }
    .form-group {
      margin-bottom: 15px;
    }
    .form-group label {
      display: block;
      margin-bottom: 5px;
      color: #b0b0b0;
    }
    .form-group input, .form-group textarea, .form-group input[type="file"] {
      width: 100%;
      padding: 10px;
      border: 1px solid #444;
      border-radius: 5px;
      background: #333;
      color: #e0e0e0;
    }
    .btn-add {
      background: #00d1b2;
      color: #1c2526;
      border: none;
      padding: 10px 20px;
      border-radius: 5px;
      cursor: pointer;
    }
    .btn-add:hover {
      background: #00b386;
    }
    .btn-view {
      background: #004aad;
      color: white;
      border: none;
      padding: 5px 10px;
      border-radius: 5px;
      cursor: pointer;
      text-decoration: none;
    }
    .btn-view:hover {
      background: #003780;
    }
    .team-list {
      list-style: none;
      padding: 0;
    }
    .team-item {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 10px;
      background: #2c3e50;
      border-radius: 5px;
      margin-bottom: 10px;
    }
    .team-item img {
      max-width: 100px;
      border-radius: 5px;
    }
    .btn-delete {
      background: #dc3545;
      color: white;
      border: none;
      padding: 5px 10px;
      border-radius: 5px;
      cursor: pointer;
    }
    .btn-delete:hover {
      background: #c82333;
    }
    @media (max-width: 768px) {
      .sidebar {
        transform: translateX(-250px);
      }
      .sidebar.active {
        transform: translateX(0);
      }
      .main-content {
        margin-left: 0;
        width: 100%;
      }
      .toggle-btn {
        display: block;
        background: #2c3e50;
        color: #e0e0e0;
        border: none;
        padding: 10px;
        cursor: pointer;
        position: fixed;
        top: 10px;
        left: 10px;
        z-index: 1000;
      }
    }
  </style>
  <script>
    document.addEventListener('DOMContentLoaded', () => {
      const toggleBtn = document.createElement('button');
      toggleBtn.innerHTML = '☰';
      toggleBtn.className = 'toggle-btn';
      document.body.appendChild(toggleBtn);
      toggleBtn.addEventListener('click', () => {
        document.querySelector('.sidebar').classList.toggle('active');
      });

      // Debug: Log View Details URLs
      document.querySelectorAll('.btn-view').forEach(link => {
        console.log('View Details URL:', link.getAttribute('href'));
      });
    });
  </script>
</head>
<body>
  <div class="content">
    <div class="dashboard-container">
      <div class="sidebar">
        <img src="/MEDIA/Logos/zllogo.png" alt="Law Firm Logo" class="logo">
        <ul>
          <li><a href="/admin/dashboard">Dashboard</a></li>
          <li><a href="/admin/notices">Manage Notices</a></li>
          <li><a href="/admin/newsletters">Manage Newsletters</a></li>
          <li><a href="/admin/team">Manage Team</a></li>
          <li><a href="/admin/practice-areas">Manage Practice Areas</a></li>
          <li><a href="/admin/events">Manage Events</a></li>
        </ul>
      </div>
      <div class="main-content">
        <div class="form-card">
          <h2>Add New Team Member</h2>
          <form method="POST" action="/admin/team" enctype="multipart/form-data">
            <div class="form-group">
              <label for="name">Name</label>
              <input type="text" id="name" name="name" required>
            </div>
            <div class="form-group">
              <label for="position">Position</label>
              <input type="text" id="position" name="position" required>
            </div>
            <div class="form-group">
              <label for="qualifications">Qualifications</label>
              <input type="text" id="qualifications" name="qualifications" required>
            </div>
            <div class="form-group">
              <label for="biography">Biography</label>
              <textarea id="biography" name="biography" required></textarea>
            </div>
            <div class="form-group">
              <label for="email">Email</label>
              <input type="email" id="email" name="email" required>
            </div>
            <div class="form-group">
              <label for="contactNumber">Contact Number</label>
              <input type="text" id="contactNumber" name="contactNumber" required>
            </div>
            <div class="form-group">
              <label for="teamPhoto">Upload Photo (optional)</label>
              <input type="file" id="teamPhoto" name="teamPhoto" accept="image/*">
            </div>
            <button type="submit" class="btn-add">Add Member</button>
          </form>
        </div>
        <div class="list-card">
          <h2>Existing Team Members</h2>
          <% if (teamMembers && teamMembers.length > 0) { %>
            <ul class="team-list">
              <% teamMembers.forEach(member => { %>
                <li class="team-item">
                  <% if (member.photoPath) { %>
                    <img src="<%= member.photoPath %>" alt="<%= member.name %>">
                  <% } %>
                  <span><%= member.name %> - <%= member.position %></span>
                  <div>
                    <a href="/team/<%= member._id.toString() %>" class="btn-view">View Details</a>
                    <form method="POST" action="/admin/team/delete/<%= member._id.toString() %>" style="display: inline;">
                      <button type="submit" class="btn-delete">Delete</button>
                    </form>
                  </div>
                </li>
              <% }) %>
            </ul>
          <% } else { %>
            <p>No team members available.</p>
          <% } %>
        </div>
        <a href="/admin/dashboard" class="btn-add" style="display: inline-block; margin-top: 20px;">Back to Dashboard</a>
      </div>
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>