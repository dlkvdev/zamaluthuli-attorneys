<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Manage Newsletters - Zama Luthuli Attorneys</title>
  <link rel="stylesheet" href="/styles.css">
  <style>
    body { background-color: #1c2526; color: #e0e0e0; font-family: 'Roboto', sans-serif; margin: 0; padding: 0; }
    .dashboard-container { display: flex; min-height: 100vh; }
    .sidebar { width: 250px; background: #2c3e50; padding: 20px 0; position: fixed; height: 100%; }
    .sidebar ul { list-style: none; padding: 0; }
    .sidebar ul li { padding: 15px 20px; }
    .sidebar ul li a { color: #e0e0e0; text-decoration: none; font-size: 1.1rem; }
    .sidebar ul li:hover { background-color: #34495e; }
    .main-content { margin-left: 250px; padding: 20px; width: calc(100% - 250px); background: #ecf0f1; }
    .form-card, .list-card { background: #34495e; padding: 20px; border-radius: 10px; margin-bottom: 20px; }
    .form-card h2, .list-card h2 { color: #00d1b2; margin-top: 0; }
    .form-group { margin-bottom: 15px; }
    .form-group label { display: block; margin-bottom: 5px; color: #b0b0b0; }
    .form-group input, .form-group input[type="file"] { width: 100%; padding: 10px; border: 1px solid #444; border-radius: 5px; background: #333; color: #e0e0e0; }
    .btn-add { background: #00d1b2; color: #1c2526; border: none; padding: 10px 20px; border-radius: 5px; cursor: pointer; }
    .btn-add:hover { background: #00b386; }
    .newsletter-list { list-style: none; padding: 0; }
    .newsletter-item { display: flex; justify-content: space-between; align-items: center; padding: 10px; background: #2c3e50; border-radius: 5px; margin-bottom: 10px; }
    .newsletter-item a { color: #00d1b2; text-decoration: none; }
    .newsletter-item a:hover { text-decoration: underline; }
    .btn-delete { background: #dc3545; color: white; border: none; padding: 5px 10px; border-radius: 5px; cursor: pointer; }
    .btn-delete:hover { background: #c82333; }
    @media (max-width: 768px) { .main-content { margin-left: 0; width: 100%; } .sidebar { display: none; } }
  </style>
</head>
<body>
  <div class="dashboard-container">
    <div class="sidebar">
      <ul>
        <li><a href="/admin">Dashboard</a></li>
        <li><a href="/admin/notices">Manage Notices</a></li>
        <li><a href="/admin/newsletters">Manage Newsletters</a></li>
        <li><a href="/admin/team">Manage Team</a></li>
        <li><a href="/admin/practice-areas">Manage Practice Areas</a></li>
        <li><a href="/admin/events">Manage Events</a></li>
      </ul>
    </div>
    <div class="main-content">
      <div class="form-card">
        <h2>Add New Newsletter</h2>
        <form method="POST" action="/admin/newsletters" enctype="multipart/form-data">
          <div class="form-group">
            <label for="title">Title</label>
            <input type="text" id="title" name="title" required>
          </div>
          <div class="form-group">
            <label for="file">Upload PDF</label>
            <input type="file" id="file" name="file" accept=".pdf" required>
          </div>
          <div class="form-group">
            <label for="date">Date (optional)</label>
            <input type="date" id="date" name="date">
          </div>
          <button type="submit" class="btn-add">Add Newsletter</button>
        </form>
      </div>
      <div class="list-card">
        <h2>Existing Newsletters</h2>
        <% if (newsletters.length > 0) { %>
          <ul class="newsletter-list">
            <% newsletters.forEach(newsletter => { %>
              <li class="newsletter-item">
                <span><%= newsletter.title %></span>
                <span>Date: <%= newsletter.date || 'N/A' %></span>
                <a href="<%= newsletter.filePath %>" target="_blank">View PDF</a>
                <form method="POST" action="/admin/newsletters/delete/<%= newsletter.id %>" style="display: inline;">
                  <button type="submit" class="btn-delete">Delete</button>
                </form>
              </li>
            <% }) %>
          </ul>
        <% } else { %>
          <p>No newsletters available.</p>
        <% } %>
      </div>
      <a href="/admin" class="btn-add" style="display: inline-block; margin-top: 20px;">Back to Dashboard</a>
    </div>
  </div>
</body>
</html>